[project]
name = "fi-comparision"
version = "0.1.0"
description = "Interactive fund history viewer backed by Morningstar scraping"
requires-python = ">=3.13"
dependencies = [
  "pandas>=2.2.3",
  "requests>=2.32.5",
]
